<%= render 'menu' %>
<div style="text-align: right; height: 45px; margin-top: 12px;">
	<% if session['specification_p1'] != 2 %><%= link_to '<button type="button" class="btn btn-success"> <span style="font-size:10px;" class="glyphicon glyphicon-plus" aria-hidden="true"></span> Agregar Servicio </button>'.html_safe, new_service_path %><% end %> <% if session['specification_p1'] == 2 %><%= link_to '<button type="button" class="btn btn-primary"> Descargar solicitud </button>'.html_safe, services_path(@service, :format => 'pdf') %> <% else %> <%= link_to '<button type="button" class="btn btn-primary"> Descargar solicitud </button>'.html_safe, services_path(@service, :format => 'xml'), :onclick => "return confirm('¿Está seguro que desea generar la solicitud? Luego de hacerlo no podrá agregar ni modificar más elementos.')" %> <% end %>
	</div>

		<%= render 'servicetable' %>

	</div>
</body>
</html>

<style>
  
#agregar {
  font-size: 14px;
}

h3 {
  position: relative;
}

</style>
<script>
if (window.location.search.replace("?", "") == "pdf=1")
{
window.location.href = "services.pdf";
}
</script>

